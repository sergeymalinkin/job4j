build:
  nodes:
#    tests-with-coverage:
      tests:
        override:
          - command: mvn clean package
#            coverage:
#              file: jacoco.xml
#              format: jacoco
      environment:
        sdkman:
          - java@12.0.2-open

