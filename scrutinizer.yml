build:
  nodes:
    tests-with-coverage:
      tests:
        override:
          - command: mvn clean package
            coverage:
              file: jacococ.xml
              format: jacoco
      environment:
        sdkman:
          - java@12.0.2-open

